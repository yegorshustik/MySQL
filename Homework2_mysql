-- 1.Напишите запрос, который выведет всю информацию о всех записях из таблицы.
SELECT *
FROM shopping_list;

-- 2. Напишите запрос, который выведет название, количество и цену товара, количество которого превышает 10 штук.
SELECT title, amount, price
FROM shopping_list
WHERE amount>10;

-- 3. Напишите запрос, который выведет всю информацию о товарах, которые были куплены в магазине Varus, в ценовом диапазоне от 10 до 30.
SELECT *
FROM shopping_list
WHERE (shop='Varus') AND (price BETWEEN 10 AND 30);

-- 4. (!) Напишите запрос, который выведет всю информацию о товарах, которые были куплены в магазинах Varus или Fozzy в количестве не менее 10 штук.
SELECT *
FROM shopping_list
WHERE (shop='Varus, Fozzy') OR (amount>10);

 -- 5. Напишите запрос, который выведет идентификатор и название товара, который начинается с символа 'B' и куплены не в магазине Varus.
SELECT id, title
FROM shopping_list
WHERE (title LIKE 'B%') AND (shop<>'Varus');

-- 6. Напишите запрос, который выведет всю информацию о товарах, которые лежат в ценовом диапазоне от 5 до 15  названия которых не заканчиваются на 't'.
SELECT id, title
FROM shopping_list
WHERE (price BETWEEN 5 AND 15) AND title NOT LIKE '%t';

--  7. Напишите запрос, который выведет идентификаторы товаров, у которых нет названия.
SELECT id
FROM shopping_list
WHERE  title IS NULL;

 -- 8. Напишите запрос, который выведет всю информацию о товарах у которых есть название, и их цена в диапазоне от 20 до 40.
SELECT *
FROM shopping_list
WHERE  (title IS NOT NULL) AND (price BETWEEN 20 AND 40);

 -- 9. Напишите запрос, который выведет все товары у которых есть имя, отсортированные в алфавитном порядке по названии.
SELECT *
FROM shopping_list
WHERE  (title IS NOT NULL) AND (price BETWEEN 20 AND 40)
ORDER by title ASC;

-- 10. Напишите запрос, который названия магазинов в которых были куплены товары, отсортированные в убывающем порядке по названию магазина. Убрать дубликаты.
SELECT DISTINCT title
FROM shopping_list
ORDER BY title DESC;
